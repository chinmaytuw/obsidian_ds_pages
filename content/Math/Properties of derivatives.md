Properties of [[derivatives]]

# Rule 1: Multiplication by a scalar:

1. Suppose we have a function y=x^2. If we multiply this by a scalar, meaning if it becomes y=2x^2, the slope will multiply by 2 as well. This is because the run stays the same but the rise is multiplied by 2.
![[1_dp3.png]]

To summarize:
![[2_dp3.png]]

# Rule 2: Sum Rule:

![[3_dp3.png]]


# Rule 3: Product rule

1. Think product, think squares. So similarly, we have something like below. There are 2 functions that increase a little in each direction. The resulting deltas give rise to 3 rectangles with varying areas. The overall rate of change then is the sum of areas of these rectangles. However, since one of the rectangles is incredibly small, we ignore it.
![[4_dp3.png]]


# Rule 4: [[Chain Rule]]

- For 2 variables:
![[5_dp3.png]]

- For 3 variables:
![[6_dp3.png]]

## Visualizing chain rule

1. Suppose we have the following scenario where we are going up a mountain and the temperature changes with height. The height changes with time as well. So we want to calc the rate of change of temp wrt time.
![[7_dp3.png]]
2. If we plot the 3 functions like below, we can see how delta t changes delta h, which in turn changes delta T.
![[8_dp3.png]]

3. AS delta t goes to 0, the rest of the deltas go to zero as well resulting in the following:
![[9_dp3.png]]